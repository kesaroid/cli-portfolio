/**
 * Reserved for a future portfolio summary command (formerly `sumfetch`).
 * Re-export a handler from `src/utils/bin/index.ts` when you wire it back.
 */
export const SUMFETCH_COMMAND = 'sumfetch' as const;
